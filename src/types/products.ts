export interface Product {
  id: string;
  name: string;
  description: string;
  price: number;
  stock: string;
  category: string,
  imagesUrls: string[];
  createdAt: string;
  updateAt: string;
}
